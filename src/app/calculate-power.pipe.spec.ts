import { CalculatePowerPipe } from './calculate-power.pipe';

describe('CalculatePowerPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculatePowerPipe();
    expect(pipe).toBeTruthy();
  });
});
